# Canonical Benchmark Tools

The final `tool_registry.json` will contain only implemented benchmark tools.
Frameworks should bind canonical names directly where possible and adapters
should normalize only unavoidable aliases.

The example registry is deliberately empty until tool ownership is confirmed.
