# Canonical Tool Input Schemas

After the registry is approved, add one Draft 2020-12 input schema named
`{tool_name}.schema.json` for every registered tool. `arguments_valid` must be
computed from these files before execution; a missing schema is not treated as
valid.
