# Code Review Findings and Fixes

This branch (`review-fixes-pr1`) changes 17 files relative to `jessica/infrastructure-schema-compat`. That's wider than the three `/code-review` findings below — it also carries a new offline dataset validator/schema (not requested by the review, bundled in because it depends on the same evaluator-key tiering) and, as of this pass, the fixes requested on the PR#5 review comment itself. This document is split accordingly so reviewers can see all of it, not just the original three findings.

## Part 1 — Original `/code-review` findings

| # | File / Line | Finding | Fix | Commit |
|---|---|---|---|---|
| 1 | `adapter/runtime.py:125` | `resolve_generation_settings()` used exact float inequality (`!=`) to flag a requested generation setting as "unsupported," so a wrapper round-tripping a value like `0.7` through its own float/pydantic coercion into `0.6999999999999998` was wrongly marked unsupported. | Added `_generation_value_matches()`, comparing floats with `math.isclose(abs_tol=1e-9)` while keeping exact comparison for int fields. Added direct unit tests for both the round-trip-noise case and a genuinely dropped setting. | `15ab762` |
| 2 | `adapter/validation.py:19` | `EVALUATOR_ONLY_KEYS` enforced a 20-word case-insensitive blocklist — including generic words like `note`, `evaluation`, `expected`, `gold`, `rubric` — as a hard, blocking gate at task-load time for every schema-v1.0 case across all three frameworks. | Split the list into `STRICT_EVALUATOR_ONLY_KEYS` (unambiguous, compound terms — still hard-blocks at runtime load) and `AMBIGUOUS_EVALUATOR_ONLY_KEYS` (common dictionary words — still flagged by the offline dataset reviewer, `scripts/validate_core_pilot.py`, but no longer crashes every framework's task load on a word collision). Wired the strict tier into `adapter/task_loader.py.task_from_dict()` so a v1.0 case with a forbidden key is rejected at load time via `validate_benchmark_case_constraints()`. | `e8dc016` |
| 3 | `frameworks/crewai_agent/run.py:150` | The entire `run_task` control flow (pre-build timing capture, model-construction error handling, `begin_run`/`finish_run` wiring, tool-log reset, early-return-on-model-error, try/except around the agent run) was duplicated nearly verbatim across `frameworks/crewai_agent/run.py`, `frameworks/langgraph_agent/run.py`, and `frameworks/openai_agents_sdk/run.py`. | Extracted the shared control flow into `run_framework_task()` in `adapter/runtime.py`. Each adapter's `run_task()` now only supplies framework-specific `build_model`/`run_model` closures; `configured_generation_settings()` stays called from each framework module so existing per-framework test mocking still works. | `3bd2a1e` |

## Part 2 — Additional scope bundled into this branch (not review findings)

| File(s) | What changed | Why it's here |
|---|---|---|
| `scripts/validate_core_pilot.py` (rewritten, +285/-?), `schemas/gold_record.schema.json` (new) | New offline validator for generated core-pilot output: schema-validates every case and gold record, cross-checks case↔gold linkage, enforces per-task/gold counts against `--expected-per-task`, validates `split_manifest.json`/`coverage_report.json` against the production contract, and calls `find_evaluator_leakage()` to fail the build if any evaluator-only key leaked into an agent-visible case. | Depends directly on the `STRICT_EVALUATOR_ONLY_KEYS`/`AMBIGUOUS_EVALUATOR_ONLY_KEYS` split from finding #2 and on the new gold-record schema, so it landed in the same branch rather than a follow-up PR. |
| `scripts/prepare_core_pilot.py` (+73/-16 relative to base, excluding the E2 fix in Part 3) | Added `_new_coverage_report()`/`_new_split_manifest()`/`_split_manifest_entries()` helpers and `total_cases`/`total_gold_records` rollups on the manifest/report, so the new validator above has real fields to check. | Support code for the new validator; not a bug fix. |
| `tests/test_generation_settings.py`, `tests/test_task_loader.py`, `tests/test_validate_core_pilot.py` (new files) | Regression coverage for findings #1–#3 above, the task-loader constraint gate, and the new validator. | — |

## Part 3 — Fixes requested on the PR#5 review comment (this pass)

| # | File / Line | Finding | Fix |
|---|---|---|---|
| 4 | `mappings/h2_urgency_mapping.json`, `scripts/validate_dataset_specs.py`, `adapter/runtime.py` (`redact_error`), `adapter/task_loader.py` (`allowed_tools`), `scripts/prepare_core_pilot.py` (`convert_e2`) | These shared/data fixes existed only on PR#6 (`review-fixes-pr2`, based on `mickey/crewai-integration`), which is a different base branch from this one. Landing them only there means LangGraph and OpenAI Agents SDK never get them, and — most importantly — `mappings/h2_urgency_mapping.json` on `main`/this branch still committed case-level gold answers (`confirmed_review_cases`, `verified_findings`) directly into a tracked file, an evaluator-data leak. | Ported the shared-file hunks from PR#6's commit `1d4efb1` onto this branch: gold answers moved to gitignored `evaluator_data/local_review_decisions/h2_owner_review_decisions.json`, `mappings/h2_urgency_mapping.json` now holds only a `_ref` pointer; `validate_dataset_specs.py` checks for the pointer instead of re-asserting the gold literals; `redact_error()` centralized in `adapter/runtime.py` and applied in `finish_run()` for all three frameworks; `adapter/task_loader.py`'s v1.0 path now defaults missing `allowed_tools` to `None` (unrestricted), matching the legacy path, instead of `[]`; `prepare_core_pilot.py`'s E2 converter now tracks `constraints_satisfied` from the actual constrained candidate set instead of hardcoding `True` whenever any candidate existed at all. CrewAI-only hunks from that commit (`frameworks/crewai_agent/run.py`/`run_core.py`, its tests) were left on PR#6 since they don't apply to this branch. |
| 5 | `adapter/runtime.py`, `run_framework_task()` | On a `build_model()` failure, the code called `unsupported_generation_settings()`, which marked *every* requested generation setting as "unsupported" in `raw_metadata`, even though the model was never constructed at all — conflating "this framework rejected setting X" with "the run never got that far." | Replaced with `failed_model_construction_settings()`, which reports `unsupported=()` (nothing was ever attempted) and a new explicit `raw_metadata.model_construction_failed` boolean, threaded through `RunContext`/`begin_run`/`finish_run`. Added `RunFrameworkTaskModelConstructionTests` in `tests/test_generation_settings.py` covering both the build-failure path (`model_construction_failed=True`, empty `unsupported_generation_settings`) and the run-failure path (`model_construction_failed=False`). |
| 6 | `scripts/validate_core_pilot.py`, case loop and gold loop | `json.loads(...)` followed immediately by `.get(...)` on the parsed value — if a case file or gold line parsed to valid-but-non-object JSON (a list, string, or number), `.get()` raised an uncaught `AttributeError` and crashed the whole validation run instead of reporting one bad record. | Added `isinstance(case, dict)` / `isinstance(row, dict)` checks right after each `json.loads()`, appending a `"expected a JSON object, found <type>"` error and `continue`-ing, matching the pattern `_load_required_artifact()` already used for `split_manifest.json`/`coverage_report.json`. Added `test_reports_non_object_case_json_instead_of_crashing` and `test_reports_non_object_gold_json_instead_of_crashing` to `tests/test_validate_core_pilot.py`. |

## Verification

All fixes (Parts 1–3) were verified by running the full offline test suite (`python -m unittest discover -s tests`) in each of the three framework-specific virtual environments:

- `.venv-crewai` — 49 tests, 0 failures, 6 skipped (LangGraph/OpenAI Agents SDK not installed in this venv)
- `.venv-langgraph` — 49 tests, 0 failures, 15 skipped (CrewAI/OpenAI Agents SDK not installed in this venv)
- `.venv-openai` — 49 tests, 0 failures, 6 skipped (CrewAI/LangGraph not installed in this venv)

No regressions were observed. Test count is higher than the previous 45 because of the new `RunFrameworkTaskModelConstructionTests` and `test_reports_non_object_*` cases added in Part 3.
