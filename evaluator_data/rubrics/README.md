# Rubrics

Place approved, versioned task-level evaluator rubrics here. These files are
not agent-visible.

H5 `clarify` and `escalate` cases are manually designed by the evaluation
owner. The example file is a blank authoring contract, not an approved case.
